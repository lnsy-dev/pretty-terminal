/**
 * Stub for @xterm/addon-ligatures under Vitest.
 *
 * The published package points its `main` field to a `.js` file that is not
 * shipped, so Vitest cannot resolve it. This stub provides the exported class
 * that the terminal component imports.
 */
export class LigaturesAddon {
  constructor() {}
}
